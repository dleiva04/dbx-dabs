bundle:
  name: DABs_demo

include:
  - resources/*.yml

variables:
  warehouse_id:
    default: 0707fbe9cb3ee407

targets:
  development:
    mode: development
    workspace:
      host: https://adb-1045986908780898.18.azuredatabricks.net
      root_path: /Datateam/${bundle.target}/${workspace.current_user.userName}/${bundle.name}
    default: true
    git:
      branch: dev

  staging:
    workspace:
      host: https://adb-1045986908780898.18.azuredatabricks.net
      root_path: /Datateam/${bundle.target}/${bundle.name}
    git:
      branch: stg

  production:
    mode: production
    workspace:
      host: https://adb-1045986908780898.18.azuredatabricks.net
      root_path: /Datateam/${bundle.target}/${bundle.name}
    git:
      branch: main
    run_as:
      user_name: david.leiva@databricks.com
